---
import IndexStackedLayout from '@/components/IndexStackedLayout';

const { API_TOKEN } = Astro.locals.runtime.env;
const apiTokenSet = API_TOKEN && API_TOKEN !== '';
---

<IndexStackedLayout client:load apiTokenSet={apiTokenSet} />
